# Documents

Place the sanitised Splunk alert export here as `splunk-alert-export.pdf` only after reviewing every page against the redaction checklist.

The raw export is not required for the repository if it contains identifiers that cannot be safely removed. Sanitised screenshots and the incident report can provide sufficient evidence of the workflow.
